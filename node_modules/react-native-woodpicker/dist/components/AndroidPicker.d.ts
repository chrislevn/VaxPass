import { ViewStyle } from "react-native";
import { PickerItem } from "../types";
export declare type Props = {
    selectedItem: any;
    disabled?: boolean;
    title?: string;
    mode?: "dialog" | "dropdown";
    renderInput: () => JSX.Element;
    renderPickerItems: () => Array<JSX.Element>;
    onItemChange: ((itemValue: PickerItem, itemIndex: number) => void) | undefined;
    containerStyle?: ViewStyle;
    customProps: {
        [key: string]: any;
    };
};
declare const AndroidPicker: ({ selectedItem, disabled, title, mode, renderInput, renderPickerItems, onItemChange, containerStyle, customProps, }: Props) => JSX.Element;
export default AndroidPicker;
