import React from "react";
import { View } from "react-native";
import { Picker as RNPicker } from "@react-native-picker/picker";
import { styles } from "../helpers/stylesHelper";
const AndroidPicker = ({ selectedItem, disabled, title, mode, renderInput, renderPickerItems, onItemChange, containerStyle, customProps, }) => {
    return (React.createElement(View, { style: containerStyle },
        renderInput(),
        React.createElement(RNPicker, Object.assign({ style: styles.androidPickerContainer, prompt: title, 
            // @ts-ignore
            onValueChange: onItemChange, selectedValue: selectedItem.value, mode: mode || "dialog", enabled: !disabled }, customProps), renderPickerItems())));
};
export default AndroidPicker;
